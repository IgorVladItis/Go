<!DOCTYPE html>

<title>Edit Moves</title>

{% block body %}
    <h1>Edit Moves</h1>

    {{ include('moves/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_moves_index') }}">back to list</a>

    {{ include('moves/_delete_form.html.twig') }}
{% endblock %}
