<!DOCTYPE html>

<title>New Moves</title>

{% block body %}
    <h1>Create new Moves</h1>

    {{ include('moves/_form.html.twig') }}

    <a href="{{ path('app_moves_index') }}">back to list</a>
{% endblock %}
